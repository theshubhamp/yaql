[workspace]
members = [".", "yaql-core", "yaql-std", "yaql-macros"]

[package]
name = "yaql"
version = "0.1.0"
edition = "2021"

[dependencies]
yaql-core = { path = "yaql-core" }
yaql-std = { path = "yaql-std" }
inventory = "0.3.24"
regex = "1"
serde_json = "1.0.91"
